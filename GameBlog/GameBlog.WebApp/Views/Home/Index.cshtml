@using Microsoft.AspNetCore.Identity
@using GameBlog.DAL.Entities
@inject UserManager<User> userManager
@{
    ViewData["Title"] = "Home";
    string name = (await userManager.GetUserAsync(User) ?? new User(){UserName = "friend :)"}).UserName;
}
<div class="jumbotron">
    <h1>
        Hello, @name
    </h1>
</div>